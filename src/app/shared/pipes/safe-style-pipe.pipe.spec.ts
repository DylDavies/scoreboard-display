import { SafeStylePipePipe } from './safe-style-pipe.pipe';

describe('SafeStylePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SafeStylePipePipe();
    expect(pipe).toBeTruthy();
  });
});
